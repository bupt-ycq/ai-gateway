# 验证记录

验证日期：2026-09-20。基础版本：New API `v1.0.0-rc.23`，提交 `0ab02020603d22e5613bc4cf46bfab06f8567769`。

## 已验证

- 官方 Linux amd64 二进制与官方校验文件一致，SHA-256：`885df6969b6937b9cd649de7461539a41b020e2390ce88c368e8452844e75e68`。
- 实际 SQLite 实例初始化、管理员鉴权、品牌设置写入及读回验证成功；重新应用未变更设置时不重复写入。
- 首页、关于页、Logo 和插图使用本地数据，首页 API 示例使用新实例地址。
- Chromium 中验证 1440、768、390 像素首页宽度没有整页横向溢出，图片加载成功；关于页也通过小屏检查。
- 自定义 HTML 被 New API 清理后，Python / Node.js / cURL 示例仍可通过点击和键盘切换。
- 深浅主题切换、首页入口跳转、管理员登录、真实控制台、渠道及令牌管理页面通过浏览器验证。
- `verify-relay.py` 使用独立临时数据库与明确标记的本地 MOCK 上游，验证普通聊天、SSE、上游鉴权和消息传递、用量日志、正数扣费，以及无效令牌返回 401；测试后清理临时实例。
- Docker Compose v2.39.4 检查基础配置和 HTTPS 合并配置通过；镜像标签存在。Shell 语法和新增 Python 脚本语法检查通过。
- 原生服务停止、重新启动、持久化数据库和会话密钥验证成功；修复了端口探测受 TIME_WAIT 影响的问题。

## 验证边界

- 尚未接入真实上游 API；模拟测试不代表真实模型可用性、输出质量或供应商计费结果。
- 当前环境没有 Docker daemon，未实际启动 Compose 容器；公网 DNS、TLS 签发和支付渠道尚未部署验证。
- 控制台数据属于独立新实例，未迁移参考站的用户、上游密钥、余额或订单。
- 使用官方已构建前后端，通过配置还原页面；本轮未修改或重新编译 New API 核心代码。

浏览器截图和机器可读检查结果位于本机 `.local/previews/`，不包含在源码包中。复验方法见同目录 README。
