# 固定与参考站相同的 New API 版本；源码构建后可改为本地镜像名。
NEW_API_IMAGE=calciumion/new-api:v1.0.0-rc.23
GATEWAY_PORT=3000
# start.sh 首次启动时生成随机值。不要填写管理员密码或上游 API Key。
SESSION_SECRET=CHANGE_ME
# 可选：自己的域名，仅填主机名，不含 https:// 或路径。
# GATEWAY_DOMAIN=gateway.example.com
